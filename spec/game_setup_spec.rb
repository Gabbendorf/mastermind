require 'spec_helper'
require_relative '../lib/mastermind'
require_relative '../lib/codemaker'
require_relative '../lib/board'

RSpec.describe Mastermind do

  # let(:codemaker) {Codemaker.new("computer", 4)}
  # let(:game_setup) {GameSetup.new(codemaker, 8)}
  #
  # it "creates the board" do
  #   board = game_setup.create_board
  #
  #   expect(board).to respond_to(:keep_track_of_results, :printable_history).with(1).argument
  # end

end
